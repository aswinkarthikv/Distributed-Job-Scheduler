// Models placeholder: ORM model mappings (e.g. Prisma schemas, Mongoose models, pg-typed classes)
// TODO: Define ORM Schema mappings
export const DatabaseModels = {
  // project, queue, job, worker
};
